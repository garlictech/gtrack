/* import { Component } from '@angular/core';

import { DynamicFormSectionComponent as NativeDynamicFormSectionComponent } from '@gtrack/shared/forms/feature';

@Component({
  selector: 'gtrack-form-section',
    templateUrl: './ui.html',
   styleUrls: ['./style.scss'],  
})
export class DynamicFormSectionComponent extends NativeDynamicFormSectionComponent {
  trackByFn(index: number): number {
    return index;
  }
}
 */