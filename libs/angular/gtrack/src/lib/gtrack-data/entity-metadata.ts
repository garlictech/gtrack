import { EntityMetadataMap } from '@ngrx/data';

const entityMetadata: EntityMetadataMap = {
  HikeGroup: {},
  Poi: {},
  Hike: {},
  Image: {},
};

export const gTrackEntityConfig = {
  entityMetadata,
};
