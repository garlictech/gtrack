{
  "version": "2018-05-29",
  "operation": "BatchGetItem",
  "tables":  {
    "gtrack-master-pois" : {
      "keys": [
        #foreach($item in ${context.arguments.ids})
          { "id": {"S" : "$item" }} #if( $foreach.hasNext ), #end
        #end
      ]
    }
  }
}
